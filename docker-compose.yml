version: '3.8'

services:
  backend:
    build:
      context: .
      target: development
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - ./history.json:/app/history.json
    environment:
      - NODE_ENV=development
    command: npm run dev